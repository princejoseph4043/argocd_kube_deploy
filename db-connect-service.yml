apiVersion: v1
kind: Service
metadata:
  name: db-connect-service
#  namespace: dev
spec:
  ports:
  - port: 80
    targetPort: nginx-port
    protocol: TCP
  selector:
    app: db-connect
  type: LoadBalancer